**** TODO Claw机器人动作模仿管理员功能，飞书需求: https://bcn9fa1lvktb.feishu.cn/wiki/RhITwOT6DiUBr0kgoXYcppZSnrh [0/7]
     产品验收标准：管理员登录 → 自动进入管理员专属页面 → 左侧导航管理用户/充值/套餐/客服 → 完整走通用户查询 → 充值（套餐+自定义）→ 交易明细查看 → 套餐生命周期管理 → 客服联系方式增删改查

***** TODO 侧边导航结构
      - 以管理员角色（admin）登录后，自动进入管理员专属页面，非管理员登录进入普通用户动作模仿页面
      - 左侧导航栏结构：顶部「视频人体动作模仿」菜单项 → 「管理员功能」分类标题 → 其下依次排列「用户管理」「用户充值」「套餐管理」「客服联系方式管理」
      - 点击各导航项切换右侧内容区，当前选中项高亮

***** TODO 用户管理
      - 表格展示 8 个字段：序号、用户名、邮箱、单位名称、联系方式、状态（正常/禁用，仅展示不切换）、最后登录时间、注册时间
      - 支持后端分页，每页条数可选 10 / 20 / 50
      - 支持按用户名或邮箱模糊搜索，回车或点击搜索按钮触发后端查询
      - 搜索结果同样支持分页，切换页码/条数时保留搜索条件

***** TODO 用户充值
      - 左侧用户列表仅展示用户名和邮箱，支持后端分页（10/20/50）和搜索（按用户名/邮箱，回车或按钮触发）
      - 点击选中用户后，右侧展示：用户名、邮箱、当前视频解析剩余次数、当前生成动作剩余次数
      - 套餐充值：下拉选择套餐 → 步进器调整数量（默认 1） → 金额自动计算（套餐单价 × 数量，不可修改） → 点击「充值」
      - 自定义充值：手动输入视频解析次数、生成动作次数、金额三个字段 → 点击「充值」
      - 充值成功后提示「充值成功」，右侧用户配额自动刷新

***** TODO 交易明细
      - 入口位于用户充值页面内，选中用户后才可查看该用户的交易明细
      - 表格展示 8 个字段：序号、时间、类型（充值/消费）、内容（套餐名称/充值说明）、视频解析次数变动、生成动作次数变动、操作人（管理员用户名）、金额
      - 支持后端分页（10/20/50）和时间范围筛选
      - 统称为「交易明细」，不区分充值记录/消费记录

***** TODO 套餐管理
      - 套餐列表展示所有套餐，每个套餐显示：套餐名称、视频解析次数、生成动作次数、价格、状态
      - 套餐三种状态：未上架（新建初始态，用户端不可见）、上架（用户端可见可购买）、下架（用户端不可见，已购用户仍可使用）
      - 点击「新建套餐」：填写套餐名称、解析次数、生成次数、价格，保存后状态为「未上架」
      - 仅未上架和下架状态的套餐可编辑（修改名称、次数、价格）；上架套餐需先下架再编辑
      - 仅未上架和下架状态的套餐可删除
      - 点击「上架/下架」可切换套餐在用户端的可见状态

***** TODO 客服联系方式管理
      - 列表展示所有联系方式，每条包含类型（微信/电话/邮箱等）和具体内容
      - 新增联系方式：填写类型和具体内容后保存
      - 编辑联系方式：修改已有记录的类型和内容
      - 删除联系方式：移除不再使用的记录
      - 修改后用户端充值页面实时展示最新客服联系方式

***** TODO 完整联调——管理员功能
      - 管理员登录 → 自动进入管理员页面 → 浏览左侧导航完整菜单结构 → 验证普通用户登录进入动作模仿页面
      - 用户管理：搜索用户 → 切换分页条数 → 翻页 → 验证 8 字段完整展示，状态仅展示不可切换
      - 用户充值：搜索选中用户 → 套餐充值（选套餐+调数量+自动计金额）→ 充值成功 → 配额自动刷新 → 切换自定义充值输入三字段 → 充值成功
      - 交易明细：选中用户 → 查看交易明细表格 → 设置时间范围筛选 → 切换分页 → 验证 8 字段完整
      - 套餐管理：新建套餐（验证初始状态为未上架） → 编辑未上架套餐 → 上架 → 验证上架状态不可编辑 → 下架 → 删除
      - 客服联系方式：新增 → 编辑 → 删除 → 切换到用户端验证实时同步
